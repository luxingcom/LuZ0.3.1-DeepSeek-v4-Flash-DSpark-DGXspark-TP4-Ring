# 修复远端同步与重启执行报告（网关 + 双机脚本）

**日期**：2026-08-04
**工作流**：工作流 4（部署执行 + 验证）
**参与成员**：Rex（SRE 工程师）/ Zhen（主理人，编排与复核）
**前置**：`fix-execute-spec-and-reasoning-2026-08-04.md`（本地修复已完成，本次为远端同步生效）

---

## 📌 TL;DR（执行摘要）

- **整体结论**：本地已完成的三项网关规范修复已**同步到远端并重启生效**（严格 404 / reasoning 透传 / 502 api_error 均验证通过）；双机启动脚本已补 `--reasoning-parser deepseek_v4`（最小变更，bash -n 通过），待 vLLM 重启后生效。
- **严重度分布**：执行完成 0 阻塞；发现 1 项测试文档认知偏差（鉴权 key 分层使用）已修正。
- **阻塞 / 非阻塞**：非阻塞。网关修复已生效；vLLM 重启（生产中断风险）待人类决策时机。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| 整体评级 | 🟢 网关修复已生效；脚本就绪待 vLLM 重启 |
| 阻塞项数量 | 0 |
| 已执行项 | 网关同步+重启+验证（阶段 A）/ 双机脚本最小变更（阶段 B） |
| 待人类决策 | vLLM 重启时机（reasoning-parser 生效前提） |
| 建议下一步 | 低峰窗口双机重启 vLLM -> WorkBuddy 验证思考折叠 -> 决策 thinking 行为参数 |

---

## 🔍 执行详情

### 阶段 A：网关代码同步 + 重启 + 验证（Rex）

| 步骤 | 结果 |
|------|------|
| 1. 备份 | ✅ `main.py.bak.20260804_prefix` (14410B) |
| 2. scp 同步 | ✅ 远端 `main.py` = 14606B，与本地修复版一致 |
| 3. 内容验证 | ✅ 行 219 严格 404 / 无 `reasoning` 剥离（仅 NOTE 注释）/ 行 249 & 316 `api_error` / 行 199 embedding `upstream_error` 保持不动 |
| 4. py_compile | ✅ PY_COMPILE_OK |
| 5. 重启 | ✅ `active (running)` since 05:08:27, PID 2889603 |
| 6. 功能验证 | ✅ 未知模型=**404** / 正常请求=**200** / 无 key=**401** |

### 阶段 B：双机脚本 reasoning-parser 最小变更（Rex）

| 步骤 | 结果 |
|------|------|
| 1. 备份 | ✅ 两机 `.bak.20260804`（worker 5805B / head 6093B） |
| 2. 插入 | ✅ 均在原行 43 后插入行 44 `  --reasoning-parser deepseek_v4 \`（python 脚本实现，规避 sed `\n` 坑）；行 42-44 三参数连续 |
| 3. 语法检查 | ✅ `bash -n` 两机通过（WORKER_OK / HEAD_OK） |
| 4. vLLM 重启 | ⏸️ 未执行（生产中断风险，时机待人类决策） |

---

## ⚠️ 重要发现：鉴权 key 分层认知修正（Rex 发现）

**现象**：任务说明中让用 `<API_KEY>-*` 做网关客户端验证，结果全 401；改用 `<API_KEY>-*`（客户端 key）后 404/200/401 全通过。

**结论（这是纵深防御的正确设计，不是故障）**：
- **8003 网关**：校验 `API_KEY = <API_KEY>-64b0374c6f2840fe`（客户端 key）——客户端调网关必须用此 key
- **8001 vLLM / 8020 嵌入**：校验 `UPSTREAM_API_KEY = <API_KEY>-...`（内部 key）——仅由网关转发时注入，客户端直连 8001 用客户 key 也 401（防泄露）
- **修正**：测试文档/脚本中「用内部 key 调网关」的认知错误，客户端验证一律使用 `<API_KEY>-*`

---

## ✅ 行动清单（按优先级排序）

| # | 行动 | 负责角色 | 紧急度 | 预期完成 |
|---|------|---------|--------|---------|
| 1 | 决策 vLLM 重启时机（低峰窗口双机重启，reasoning-parser 生效） | 人类负责人 | P0 | 待定 |
| 2 | 重启后验证 `reasoning_content` 字段返回（WorkBuddy 思考折叠） | 用户 | P0 | 重启后 |
| 3 | 修正测试文档/脚本中鉴权 key 使用认知（客户端=<API_KEY>，内部=<API_KEY>） | Testing | P1 | 本周 |
| 4 | 更新 systemd unit 描述 v1.3.0 -> v1.4.0（仅描述文本） | SRE | P2 | 按需 |
| 5 | 决策是否补 `thinking=true` / `reasoning_effort=max` 行为参数（沿用前报告决策点） | 人类负责人 | P1 | 重启验证后 |

---

## ⚠️ 待完善 / 已知局限

- vLLM 容器未重启，`--reasoning-parser deepseek_v4` 尚未生效；重启窗口约 5-15 分钟冷启动（此前经验），期间 8001 推理服务中断。
- systemd unit 描述文本仍为 v1.3.0（不影响功能）。
- 回滚路径：`cp main.py.bak.20260804_prefix` 覆盖远端 main.py 即可回滚网关。

---

## 📚 数据来源 & 成员产出索引

- **Rex（SRE 工程师）原始产出**：阶段 A 六步执行结果 + 阶段 B 四步执行结果 + key 分层鉴权认知修正 + 遗留风险
- **前置报告**：`fix-execute-spec-and-reasoning-2026-08-04.md`（本地修复）、`code-review-api-spec-compliance-2026-08-04.md`（规范审查）、`diagnostic-thinking-collapse-failure-2026-08-04.md`（根因诊断）

---

> 本报告由工程保障团队 AI 协作生成，关键决策（vLLM 重启时机、thinking 行为参数）请由人类工程负责人复核。
