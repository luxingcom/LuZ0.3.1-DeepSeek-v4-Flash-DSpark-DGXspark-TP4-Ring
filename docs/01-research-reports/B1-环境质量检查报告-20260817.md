# B1 环境质量检查报告

**日期**：2026-08-17 ｜ **执行**：SRE Rex ｜ **对象**：DGX Spark 四机（node01-04）TP4 vLLM 生产
**背景**：B1（NCCL_MAX_NCHANNELS 16→4）已固化，集群以 B1 配置运行 healthy ~1.4h（容器 10:18-19 UTC 启动）
**方式**：只读/最小干预全面检查（沿用 8 大项综合治理标准），敏感信息脱敏

---

## 0. 结论摘要

| 检查项 | 结果 | 状态 |
|---|---|---|
| 1 残留检查 | 无 Exited/测试残留、0 dangling 镜像、/tmp 无大文件 | ✅ |
| 2 日志健康 | journald 500M 限额生效、logrotate 正常、近 1h 0 error/0 110 | ✅ |
| 3 双机镜像 | mirror_to_02.sh 已执行，02 head 脚本 B1 同步完成 | ✅ |
| 4 服务自恢复 | **发现并修复 A/B 窗口遗留：四机 monitor 服务被停**，已恢复 active | ⚠️→✅ |
| 5 环境一致性 | 四机启动脚本+容器 env 均 MIN/MAX=4 | ✅ |
| 6 磁盘 | 01=18%（达标）、02=36%、03=32%、04=31% | ✅ |
| 7 安全抽查 | 脚本区 0 明文密码 | ✅ |
| 8 报告归档 | 本报告（本地+服务器双份） | ✅ |

**关键发现（本次治理唯一异常）**：A/B 窗口（09:31 UTC）停止的四机 monitor 自愈服务（vllm-tp4-head/worker.service）在 B1 固化后**未恢复**。容器由 start_tp4_cluster.sh 直接拉起，healthcheck.timer 仅剩只读探测，`docker rm -f` 触发后无人重建容器。本次已以 sudo 启动恢复四机 monitor，容器零扰动，自愈链重新闭环。

---

## 1. 残留检查

- **docker ps -a**：四机无 Exited/Stopped 测试残留；仅生产容器：
  - 01：vllm-tp4-rank0（healthy）+ aicad 服务栈（redis/postgres/fw/grafana/neo4j/minio + dcgm/node-exporter）
  - 02：vllm-tp4-rank1（healthy）+ litellm-proxy/registry/prometheus/postgres/redis/litellm-pg/grafana/neo4j/alertmanager/minio + dcgm/node-exporter
  - 03：vllm-tp4-rank3（healthy）+ anemll-embed-8022（SGL 环境保留）+ dcgm/node-exporter
  - 04：vllm-tp4-rank2（healthy）+ anemll-embed-8022（SGL 环境保留）+ dcgm/node-exporter
- **dangling 镜像**：四机均为 0
- **/tmp 大文件（>100M）**：四机均无

## 2. 日志健康

- **journald 占用**：01=481.7M / 02=490.8M / 03=504.1M / 04=514.2M —— 均被限制在 500M 附近封顶
- **限额配置**：四机 `/etc/systemd/journald.conf.d/zz-sre-limit.conf`（SystemMaxUse=500M）在位生效
- **logrotate**：四机 logrotate.timer active（每日 00:00 轮转）
- **vllm 日志**：实际走 docker json-file 驱动，`log-driver=json-file` + `max-size=100m` + `max-file=5`（daemon.json 确认）；当前 vllm 日志 232K（健康小）
- **近 1h 错误**：四机 vllm 容器日志 0 error / 0 110 / 0 timeout；journald `-p err` 仅含本次巡检自身 sudo 探测噪音；02 有 networkd-wait-online Timeout（启动期良性）、04 有 xdg-autostart-generator File exists（桌面自启良性），均与 vLLM/NCCL 无关

## 3. 双机镜像（01→02）

- `mirror_to_02.sh` 已执行：exit 0，耗时 3s（增量），log 见 `<INSTALL_DIR>/scripts/maintenance/mirror.log`
- **同步前差异**：02 的 `start_tp4_head.sh` 仍为 MAX_NCHANNELS=16（08:10 旧版，B1 后未同步）
- **同步后**：02 head 脚本已更新为 MAX=4；`.bak-ncclB1` 四机齐备（01/02 head + 02/03/04 worker）
- **md5 一致性**：
  - start_tp4_head.sh：01 == 02（9689b6d5…）
  - healthcheck.sh / healthcheck-rebuild.sh：四机一致（64cef4d6… / 270df791…）
  - start_tp4_worker.sh：02 与 03/04 不同（985f65e5 vs 0b29ca9c），经 diff 仅 NFS 挂载点判断差异（02=/mnt/models-nfs，03/04=<MODELS_DIR>），节点特性、非 B1 相关

## 4. 服务自恢复 ⚠️→✅（本次关键治理项）

- **发现**：四机 `vllm-tp4-head.service`（01）/`vllm-tp4-worker.service`（02/03/04）均 inactive（dead），09:31:2x-51 UTC 被 SIGTERM 停止（A/B 窗口启动时停用），B1 固化后未恢复；期间容器由 `start_tp4_cluster.sh` 直接拉起
- **影响评估**：healthcheck.timer 仍 60s 探测，但 healthcheck-rebuild 触发 `docker rm -f` 后无 monitor `docker wait` 跟随 → 容器不会被重建，自愈链断裂
- **治理动作**：sudo `systemctl start` 四机 monitor 服务，均已恢复 **active/running**（MainPID 正常，`docker wait` 附着已运行容器）；容器零扰动（仍 Up About an hour healthy），healthcheck 探针仍 exit=0
- **自愈链现状**：
  - head 01：vllm-healthcheck.timer（60s）active → healthcheck-rebuild.sh（探针+冷却 1800s）✅
  - 四机 monitor：Restart=always，容器退出 → docker wait 返回 → exit 1 → systemd 重建 ✅
  - NRestarts 为历史累积（head=3 / worker=6/9/11），B1 重启后无新增异常

## 5. 环境一致性

- **启动脚本**：01 head L116 / 02 worker L121 / 03 worker L121 / 04 worker L121 均 `MIN_NCHANNELS=4`、`MAX_NCHANNELS=4`
- **容器 env**（docker inspect 四机）：`NCCL_MIN_NCHANNELS=4`、`NCCL_MAX_NCHANNELS=4`、`NCCL_ALGO=RING`、`NCCL_NET_PLUGIN=none`、`NCCL_BUFFSIZE=8388608`、`NCCL_TUNER_THRESHOLD=40960`、无 NCCL_PROTO —— 四机一致
- **B1 备份**：`.bak-ncclB1` 四机在位（01/02 head，02/03/04 worker）

## 6. 磁盘

| 节点 | 根分区 | 已用 | 可用 | 已用% | <INSTALL_DIR> |
|---|---|---|---|---|---|
| 01 | 3.6T | 603G | 2.9T | **18%**（达标） | 41G（39G backup + 1.9G build） |
| 02 | 3.6T | 1.3T | 2.2T | 36% | 62G |
| 03 | 916G | 273G | 597G | 32% | 6.4G |
| 04 | 916G | 270G | 600G | 31% | 6.4G |

大目录 TOP 均为 backup/镜像/模型构建产物，无异常膨胀。

## 7. 安全抽查

- `<INSTALL_DIR>/scripts/` 明文密码（AS12\*\*\*\*\*\*）匹配：四机均 **0**
- B1 脚本改动（start_tp4_head/worker.sh）后复验通过，无新增敏感信息

## 8. 报告归档

- 本报告双份：本地 `deliverables/engineering-assurance/` + 服务器 `<INSTALL_DIR>/docs/`

---

## 9. 遗留与建议

1. **monitor 自愈恢复已执行**；「窗口结束恢复 monitor」已作为**强制检查点**写入 A/B 窗口 SOP（`nccl-p1p2p4-ab-window-plan-2026-08-17.md` §4 终态恢复 + §6 确认项），后续窗口按 SOP 执行防再遗漏
2. **02 磁盘 36%** 高于 01（18%），源于 02 为镜像仓库/监控栈节点，暂无需治理；可持续观察
3. **nccl-tests 窗口已执行（2026-08-17 13:5x）**：368KB 实测 103.5µs oop / 99.8µs ip（优于外推 120-130µs）；tuner 路由复测正常；窗口后 vLLM 重启收敛 + monitor 按 SOP 恢复（详见《B1-兼容性校验报告》§8）
4. 生产库 2be94172 未触碰；其余组件本次巡检零改动（除 monitor 服务恢复）

---
*敏感信息已脱敏；巡检时间 2026-08-17 11:2x-11:4x UTC*
