# LLM 服务停机状态备忘（配合视频工作流部署）

**日期**：2026-08-06 23:55
**操作性质**：其他团队部署视频工作流，LLM 主服务停机释放 GPU；embed 服务保留在线

---

## 📌 当前状态确认（实测）

| 服务 | 状态 | 证据 |
|------|------|------|
| vLLM head（8001） | ✅ 已停机 | vllm-envE-node `Exited (0)`（57 分钟前正常退出）；8001 未监听 |
| vLLM worker | ✅ 已停机 | vllm-envE-worker 容器已移除 |
| **主模型权重** | ✅ 已卸载 | 双机 vLLM 停 → 权重（~79GB/机）随容器释放给视频工作流 |
| **embed 服务（8020）** | 🟢 **在线** | embed-qwen3-gpu `Up 14 hours`；直连 8020 = 200 |
| 4000 embeddings | 🟢 200 | LiteLLM → 8020 链路正常 |
| 8003 embeddings | 🟢 200 | 8003 网关 → 8020 链路正常 |
| 4000/8003 chat 路由 | ⚠️ 预期失败 | 后端 vLLM 停（500/502），**符合预期** |

## 🔑 关键结论

1. **LLM 停机已完成且干净**：head 正常退出（Exited 0）、worker 容器移除、主权重已卸载释放 GPU
2. **embed 全链路在线**：直连 8020 / 4000 / 8003 三路 embeddings 均 200——视频工作流可正常使用 embed
3. **chat 类调用当前不可用**（预期）——依赖 LLM 的应用需等恢复

## 🔄 恢复预案（固化）

- **一键拉起 LLM**（head 机执行）：`bash ~/start_v026r_cluster.sh`（编排脚本：head 先 → 轮询 25000 → worker 后 → 轮询 8001，NCCL 修复后 3/3 稳定）
- 恢复后全链路验证：8001/8003/4000 = 200 + e2e chat + embeddings
- embed 服务无需操作（全程未动）

## ⚠️ 注意

- 视频工作流与 LLM 共享 GPU（121GB 统一内存）——恢复 LLM 前需确认视频工作流已释放 GPU 资源（LLM 需 ~98GB/机）
- Runbook（runbook-dspark-vllm-2026-08-06.md）为权威运维入口，恢复流程见其 §2

---

> 本备忘由工程保障团队生成，供恢复时对照。LLM 恢复时机由人类工程负责人协调。
