# 近期改动复查报告（2026-08-10 22:59）

> 范围：B2（01 Grafana 对齐）、B3（01 Prometheus 停用）、B6（编排脚本竞态修复）、A1（iptables 固化）
> 方法：四台 SSH 远程实测，逐项核对文件内容、容器状态、运行时行为
> 结论：**4 项改动全部真实有效，无错误级问题**；发现 2 项待办/风险 + 1 项文档勘误

---

## 一、B2 01 Grafana allowUiUpdates 对齐 — ✅ 真实有效

| 核对点 | 结果 |
|---|---|
| host 文件 `/opt/aicad/monitoring/grafana/provisioning/dashboards/aicad.yml` | `10: allowUiUpdates: false` ✅ |
| 容器内文件（挂载 `/etc/grafana/provisioning`） | `10: allowUiUpdates: false` ✅ 与 host 一致 |
| 容器状态 | `aicad-grafana-1` Up（22:40 重启生效，provisioning 日志正常）✅ |
| 02 对照 | `allowUiUpdates: false` ✅ 双机一致 |

## 二、B3 01 Prometheus/alertmanager 停用 — ✅ 真实有效

| 核对点 | 结果 |
|---|---|
| `aicad-prometheus-1` | Exited(0)，restart=no ✅ |
| `aicad-alertmanager-1` | Exited(0)，restart=no ✅ |
| compose 文件级 | 116-118 `prometheus: restart: "no"`、131-133 `alertmanager: restart: "no"` ✅ |
| compose 校验 | `docker compose config -q` 通过 ✅ |
| 02 监控栈 | prometheus/grafana/alertmanager 全 Up，未受影响 ✅ |

## 三、B6 编排脚本竞态修复 — ✅ 修改正确，⚠️ 未经真实运行验证

| 核对点 | 结果 |
|---|---|
| 语法 | `bash -n` SYNTAX-OK ✅ |
| 行数 | 156（预期 153→156）✅ |
| 旧前置检查 | 原第 50 行区域已移除，改为注释说明 ✅ |
| 新验证块顺序 | 容器清理（77/81 行）→ 清理确认（83 行）→ 8001 占用验证（84-89 行）✅ |
| 脚本 mtime | 2026-08-10 22:42:41 |
| vllm-cluster.service | ExecStart 指向该脚本，service active（21:53 曾成功执行，status=0/SUCCESS，READY 310s）|

**⚠️ 风险**：脚本修改时间（22:42）晚于 vllm-cluster 最后一次执行（21:48-21:53），**修复后脚本尚未经真实启动流程验证**。需下次 01 重启实测（与既有待办一致）。TP2 当前运行中的实例由修复前脚本拉起，不影响当前运行。

## 四、A1 iptables rules.v4 固化 — ✅ 真实有效

| 核对点 | 01 | 02 |
|---|---|---|
| save 时间戳 | 8/10 22:36:54 | 8/10 14:36:55（UTC = 22:36 CST）✅ 对齐 |
| 备份 `.bak-20260810-a1` | 存在 | 存在 |
| 固化目标规则 | `PREROUTING -d <DOCKER_IP>/32 ! -i br-0631edae6a82 -j DROP` 已入文件 ✅ | `PREROUTING -d <DOCKER_IP>/32 ! -i br-0eed6e5cd22f -j DROP` 已入文件 ✅ |
| `iptables-restore --test` | 通过（无错误输出）✅ | 通过 ✅ |
| 其余 diff | 仅计数器 + docker 容器 IP 重排（动态，正常）| 同左 ✅ |

**澄清**：01 的 FW_25000 链 diff（<RING_SUBNET> 对端 → <RING_SUBNET>/24）是 **8/9 21:12 narrow 操作的旧改动**，非 A1 引入；<NODE_IP> 的放行在独立 INPUT/OUTPUT 全 TCP 规则中，live 与 file 一致，**无数据丢失**。

## 五、核心持久化与功能指标 — ✅ 全部有效

| 项目 | 状态 |
|---|---|
| grub isolcpus=0-4 | 01/03/04 ✅ |
| rp_filter=1 | 01/03/04 ✅ |
| mlx-irq-pin（IRQ 绑核） | 01/03/04 active ✅ |
| mlnx-qos | 01/04 active ✅；**03 未部署（unit 不存在）= 预期**，QoS 部署范围本就是 01/02/04 |
| nvidia-persistenced | 01/02/03/04 active ✅ |
| netplan 97-roce-mtu.yaml | 01/03/04 ✅ |
| vllm-cluster.service | 01 active ✅ |
| TP2 API :8001 | /health=200 ✅ |
| embed（anemll-8022） | 03/04 Up 2h ✅ |
| litellm（02）| liveliness="I'm alive!" / readiness=healthy（db connected, 1.83.7）✅；近 1h 日志 12 条 error 均为认证拒绝（无 key/假 key），属正常安全行为 |

## 六、脚本 MD5 一致性 — ✅

| 脚本 | 结论 |
|---|---|
| check_vllm_script.sh | 四台一致 `f76d2bf8...` ✅ |
| start_embed_8022.sh | 四台一致 `b0ebc043...` ✅ |
| start_worker_groupB.sh | 03=04 一致 `cd110fb5...` ✅ |
| head_v026r（01）/ worker_v026r（02）/ head_groupB（03） | 各自唯一副本（角色不同文件）✅ |

---

## ⚠️ 发现的问题与建议（无错误级，3 项）

1. **01 Grafana 面板已无数据源**（B3 副作用）：01 Grafana datasource 指向容器内 `prometheus:9090`，实测容器内 `bad address`（prometheus 已停）→ 01 Grafana 3000 端口仍在运行但所有面板无数据。**待决策**：① 改 datasource 指向 02 Prometheus（<NODE_IP>:9090）；② 一并停用 01 Grafana；③ 只用 02 监控面板。
2. **B6 修复后未真实运行验证**：需下次 01 重启实测 boot 全流程（已列待办，勿关闭）。
3. **编排脚本无本机副本**：`start_v026r_cluster.sh` 仅存在于 01 服务器（含 2 个 .bak），本机 deliverables 无归档 → 建议补一份本地副本防丢失。

## 文档勘误

- 既有文档/记忆中的脚本路径 `/opt/aicad/prod/scripts/` 应为 **`<INSTALL_DIR>/scripts/`**（连字符），四台实际目录均为后者。

## 待办更新

- [x] 近期改动（B2/B3/B6/A1）真实有效性复查（2026-08-10）
- [ ] B6 修复后首次真实启动验证（01 下次重启）
- [ ] 01 Grafana 数据源处置决策（指向 02 / 停用 / 只用 02）
- [ ] start_v026r_cluster.sh 补本机归档
