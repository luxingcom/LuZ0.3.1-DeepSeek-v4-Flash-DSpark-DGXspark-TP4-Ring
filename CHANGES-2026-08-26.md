# CHANGES · 2026-08-26 — 生产加固 + 8192 批次令牌 + 四轮拦截修复

**来源**：工程保障团队 · 事故衍生生产加固（克隆环境 RoCE/GID 事故预防）
**提交人**：SRE（Rex）
**基线**：start_tp4_cluster.sh v1.5-r12 / start_tp4_head.sh v1.5-r11 / start_tp4_worker.sh v1.5-r12（2026-08-25 稳态）

---

## 1. 参数变更：`--max-num-batched-tokens` 4096 → 8192

- `start_tp4_head.sh`（L75）与 `start_tp4_worker.sh`（L71）将单步批 token 预算从 4096 提升至 **8192**。
- 目的：解决并发测试下"请求不能并行"问题，使多请求在单步批内合批处理。
- 并发验证结论（testing_final_20260826）：
  - **✅ 8192 生效，真并行确认**：conc≤12 且非极端负载下 `running>=2` 样本 892/900（99.1%），并行总时长 891s/899s，`preemptions_total=0`。
  - `running==12`（满 max-num-seqs 位）579 样本；`running>12` 0（受 `max-num-seqs=12` 硬顶）。
  - conc=12 时 agg_prefill_p50 ≈ 3000+（12 请求合批吞吐）；所有完成组合 0 error / 0 超时 / 0 抢占。
  - **⚠️ 异常**：conc=16 + ctx=4096（长 prefill）触发引擎卡死（`shm_broadcast.py:705 No available shared memory broadcast block found in 60 seconds`），EngineCore 阻塞挂起（CPU 3.3%、SLsl），全引擎不可用。无 OOM / 无 NCCL 错误 / 无容器重启。
  - **稳定性结论：未通过**——8192 收益在 conc≤12 档确认；16 档卡死需后端/架构师定位 shm_broadcast 死锁（与 `--scheduling-policy priority`、speculative `dspark` 交互），修复前建议生产保持 conc≤12 档。
  - **自愈盲区**：Docker HEALTHCHECK 仍为旧 `curl /health` 探针（只查 HTTP 200），未接入硬化活性探针；`healthcheck_hardened.sh` 已实测可检出本次卡死（rc=28 → 疑似卡死），但尚未挂到 Docker/systemd 任一环。**本次推送的 hardened 脚本即为闭合此盲区的交付物，落地时需接入。**

## 2. 生产加固（预检 / 崩溃记录 / 降级）

新增 `scripts/hardening/`（6 个加固脚本，交付后四机 md5 一致）：

| 脚本 | 用途 |
|------|------|
| `preflight_roce_gid.sh` | 连接建立前 RoCE GID 布局预检（空洞/IPv4 RoCEv2/index3 子网一致性）；布局异常 → `exit 3` No-Go 阻断启动（P0 硬门）。支持 `--degrade`、`--expect-index N`、`--peers host:idx`。 |
| `probe_gid_index.sh` | 实测当前 RoCEv2/IPv4 GID 实际 index，输出建议 `NCCL_IB_GID_INDEX`（数值 / -1 动态 / REMOVE）。 |
| `gid_index_env.sh` | start_tp4_*.sh 可 source 的 env 决策片段：实测后注入 `NCCL_IB_GID_INDEX`（动态/-1 或实际值），含降级栅栏。 |
| `crash_dump.sh` | 崩溃指纹留存（systemd ExecStopPost 调用）：dump docker logs + dmesg 到 `crash-<ts>/` 持久目录。 |
| `healthcheck_hardened.sh` | 加固守卫：推理活性探针（小请求带超时判定卡死，闭合"卡死 100s+ 仍判 healthy"盲区）+ 保留冷启动宽限。 |
| `watchdog_hardened.sh` | 加固看门狗：加载期/运行期分段 + 单位时间 NV_ERR 新增速率窗口 + carrier flap 计数探针。 |

核心变更（对应克隆环境事故分层预防）：
- **移除写死 `NCCL_IB_GID_INDEX=3`** → 改经 `gid_index_env.sh` source 注入实测值（克隆严禁照抄生产 index）。
- **预检 fail-fast 前置到 `docker run` 之前**（exit3/4），不再"等容器起来再诊断"。

## 3. 四轮拦截修复（QA 交叉审查 + 实测拦截，全部闭合）

| # | 修复点 | 内容 |
|---|--------|------|
| 1 | **判据3 ring 豁免** | `start_tp4_cluster.sh` STEP0 GID 空洞枚举改为仅 index0-3；空槽（index>3 未配置）不再误判 HOLE，避免生产 4 口 ring 必被 exit3 误拦。判据3 子网比较改 GID 末 32bit（IPv4 段）增强区分力。 |
| 2 | **MD5 转义** | `start_tp4_cluster.sh` RINGONLY `libnccl.so.2` MD5 检测片段 ANSI-C quoting+eval 在 `set -u` 下将 `awk $1` 提前展开导致 UNREACH 误报；改 `awk "{print \$1}"` 经 eval/ssh 两路径正确输出 hash（实测通过）。 |
| 3 | **0.1 段枚举（NV_ERR 增量窗口）** | `watchdog_hardened.sh` 去掉 `--since` 窗口计数，改全量累计行数 `CUR_NV - LAST_NV` 求增量，修复"NEW_ERR≈0 恒不触发、卡死期看门狗静默"的阻断级问题。 |
| 4 | **worker 检查点1a 口名门** | `start_tp4_worker.sh` 遍历 `/sys/class/net` 枚举 RoCE netdev（原 `/sys/class/infiniband` 恒不匹配 → 必 exit4 阻断）；校验 `rocep1s0f0/rocep1s0f1/roceP2p1s0f0/roceP2p1s0f1` 存在且无预期外口名，失配 → `[preflight-FAIL]` exit4。 |
| + | **patch-v026 同步** | 修复期间与 patch-v026 基线同步，保持 `--long-prefill-token-threshold 4096`、DSpark MTP n7、W4A4 等既有参数不被破坏。 |

QA 判定：修复前 4 阻断 + 8 健壮性 → 修复后 **🟢 已闭合可上生产**（9 脚本 bash -n 全过；R1/I5/HR 有 mock 实测）。

## 4. 落地上生产前必做（QA 持久化清单摘要）

1. 6 加固脚本 + 3 integrated 启动脚本 cp 到每机 `/opt/aicad-prod/scripts/` + `chmod +x`；四机同版本 + md5sum 记录。
2. 更新 systemd unit：3 个 .service 加 `ExecStopPost=.../crash_dump.sh vllm-tp4-rank{N}` + `daemon-reload && enable`。
3. 装 watchdog timer（`watchdog_hardened.timer` + unit，`enable --now`）；healthcheck_hardened 接 docker `--health-cmd`（闭合卡死自愈盲区）。
4. 改造前留 `.bak-<tag>` 可秒回滚；`check_vllm_script.sh` 通过后签字。
5. 重启验证：改后首启核验（GID 实际注入、nccl-*.log 生成、守卫无误杀）。

## 5. 相关文档（本次同步推送）

- `docs/07-deployment/INTEGRATION-hardening-2026-08-26.md` — 各脚本插入位置 / .bak 留档 / 验证方法
- `docs/07-deployment/DEPLOY_ASSIST-hardening-2026-08-26.md` — 预检内嵌位置对照 + 克隆不一致首次部署重建 6 步
- `docs/07-deployment/DEPLOY_RECORD_四机落地_20260826.md` — 四机落地证据（md5 一致 + 8192 在位）
- `docs/04-issues/incident-clone-roce-prevention-2026-08-25.md` — 克隆环境事故预防方案
- `docs/01-research-reports/hardening-design-2026-08-26.md` — 加固方案设计
- `docs/06-verification/prod-hardening-delivery-2026-08-26.md` — 生产加固交付
- `docs/06-verification/qa-hardening-review-2026-08-26.md` — QA 交叉审查与修复验证

> 仓库既有脚本 `scripts/server-production/start_tp4_head.sh`（v1.5-r11）与 `start_tp4_cluster.sh`（v1.5-r12）已被本批 integrated 最终版替换，旧版保留为 `.bak-20260826`。
