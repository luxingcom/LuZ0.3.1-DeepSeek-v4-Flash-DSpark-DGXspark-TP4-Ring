# build artifacts / scratch (never release)
__pycache__/
*.pyc
*.pyo
*.zip
*.tar.gz
*.tgz
*.so
*.bin
*.log
*.ptx
*.sass.txt

# staging of unredacted server fetch (organized copies already in repo)
_server_fetch/
_head_scan*/
# private redaction patterns (contains real secrets - never commit)
redact-patterns.json
# raw redaction map (contains real secret regexes - never commit; ship REDACTION-MAP.md instead)
REDACTION-MAP.csv
# scratch verification logs
_*.txt
_build_manifest.txt

# OS
.DS_Store
Thumbs.db
